# ---------------------------------------------------------------
# Plantilla de variables de entorno — copia este archivo a .env
# y rellena tu clave real. El archivo .env está en .gitignore y
# NUNCA debe subirse al repositorio.
#
#   cp .env.example .env
# ---------------------------------------------------------------

# API key de Google Maps (Geocoding API + Maps JavaScript API)
# Obtén la tuya en https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API_KEY=TU_API_KEY_AQUÍ
